matrix:
  include:
  - os: osx
    sudo: enabled
    before_install:
      - brew update
    script: ./bootstrap.sh -a -p mac_test_short

  - os: osx
    sudo: enabled
    before_install:
      - brew update
    script: travis_wait 60 ./bootstrap.sh -a -p mac_test_full

  - os: osx
    sudo: enabled
    before_install:
      - brew update
    script: ./bootstrap.sh -a -p mac_second_account

  - os: linux
    script: ./bootstrap.sh