---

- name: Install Anaconda
  homebrew:
    name: anaconda
    state: present
  become: false
  ignore_errors: true

- blockinfile:
    dest: "{{ home }}/.bashrc"
    marker: "### {mark} Anaconda / Jupyter ML Notebooks"
    content: "{{ anaconda__path }}"
    create: true
  become: false
  ignore_errors: true

- blockinfile:
    dest: "{{ home }}/.zshrc"
    marker: "### {mark} Anaconda / Jupyter ML Notebooks"
    content: "{{ anaconda__path }}"
    create: true
  become: false
  ignore_errors: true
