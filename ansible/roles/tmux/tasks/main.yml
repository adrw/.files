---

  - file:
      src: "{{ roles_dir }}/tmux/files/.tmux.conf"
      dest: "{{ home }}/.tmux.conf"
      state: link
      force: yes
    # with_items:
    #   - { src: '.gitconfig', dest: '{{ home }}' }
